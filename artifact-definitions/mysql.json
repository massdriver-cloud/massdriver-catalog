{
  "$md": {
    "label": "Example MySQL (edit in ./artifact-definitions/mysql.json)",
    "name": "mysql"
  },
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Example MySQL artifact - customize this schema to match your database outputs",
  "properties": {
    "connection": {
      "description": "Everything needed to connect to the database",
      "properties": {
        "database": {
          "title": "Database Name",
          "type": "string"
        },
        "hostname": {
          "title": "Hostname",
          "type": "string"
        },
        "password": {
          "$md.sensitive": true,
          "title": "Password",
          "type": "string"
        },
        "port": {
          "default": 3306,
          "title": "Port",
          "type": "integer"
        },
        "username": {
          "title": "Username",
          "type": "string"
        }
      },
      "required": ["hostname", "port", "database", "username", "password"],
      "title": "Connection Details",
      "type": "object"
    },
    "infrastructure": {
      "properties": {
        "database_id": {
          "description": "Unique identifier for the database instance",
          "title": "Database ID",
          "type": "string"
        }
      },
      "required": ["database_id"],
      "title": "Infrastructure Details",
      "type": "object"
    }
  },
  "required": ["connection", "infrastructure"],
  "title": "Example MySQL Database",
  "type": "object"
}
