name: application
label: Application

schema:
  title: Application
  description: Generic application template for customization or extension
  type: object
  required:
    - name
  properties:
    deployment_id:
      title: Deployment ID
      description: Unique deployment identifier
      type: string
    name:
      title: Name
      description: Application name
      type: string
    service_url:
      title: Service URL
      description: Base URL for the application service
      type: string
    tags:
      title: Tags
      description: Resource tags
      type: object
      additionalProperties:
        type: string