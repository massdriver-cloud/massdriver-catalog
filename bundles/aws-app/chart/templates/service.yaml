apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-app.fullname" . }}
  labels:
    {{- include "aws-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-app.selectorLabels" . | nindent 4 }}
