apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aws-app.fullname" . }}
  labels:
    {{- include "aws-app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "aws-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aws-app.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
