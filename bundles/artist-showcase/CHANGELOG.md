# Changelog

## 0.0.0

Initial release.

- Public roster page reading one granted table through a read-only login
- No schema of its own and no write path anywhere
- Marked `exposure: external`, so only a developer or operator can place it
