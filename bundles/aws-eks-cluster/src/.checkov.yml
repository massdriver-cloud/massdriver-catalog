skip-check:
  # SG is attached to the control-plane ENIs (a managed AWS service) — the
  # worker nodes get their own security group with its own rules; this one
  # has no attacker-controlled compute behind it.
  - CKV_AWS_382
