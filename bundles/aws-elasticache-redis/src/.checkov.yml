skip-check:
  # CKV_AWS_382: "Ensure no security groups allow egress from 0.0.0.0:0 to port -1"
  # ElastiCache needs outbound for AWS services (CloudWatch, SSM). Restricted ingress is the control.
  - CKV_AWS_382

  # CKV_AWS_191: "Ensure ElastiCache Cache Clusters are using TLS"
  # Transit encryption is configurable via params. Dev/caching preset may run without TLS for simplicity.
  # halt_on_failure in steps config enforces this in production environments.
  - CKV_AWS_191
