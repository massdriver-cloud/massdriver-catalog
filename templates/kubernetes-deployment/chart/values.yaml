# Default values - overridden by params via values.jq
# This file documents the expected structure

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

replicas: 1
port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

env: []
